<UserControl x:Class="SignalrClient.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SignalrClient.Views"
             mc:Ignorable="d" 
             MinHeight="400"
             MinWidth="600"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="Green">
        </Grid>

        <GridSplitter Grid.Row="1" HorizontalAlignment="Stretch" Height="5"></GridSplitter>

        <TabControl Grid.Row="2" Background="AntiqueWhite">
            <TabItem Header="Execute">
                <local:ExecuteView x:Name="Request"></local:ExecuteView>
            </TabItem>
            <TabItem Header="Subscribe">

            </TabItem>
            <TabItem Header="Code">

            </TabItem>
        </TabControl>

    </Grid>
</UserControl>
